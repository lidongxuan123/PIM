const Service = require('egg').Service
class SessionService extends Service {
    async setsession() {
        this.ctx.session.token =
        this.ctx.session.token =
        this.ctx.session.token =
        this.ctx.session.token =
    }
}